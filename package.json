{
  "name": "kakuyomu",
  "version": "1.0.0",
  "private": true,
  "description": "Google Apps Script project managed with clasp",
  "scripts": {
    "push": "clasp push",
    "pull": "clasp pull",
    "open": "clasp open",
    "deploy": "clasp push && clasp deploy",
    "status": "clasp status"
  },
  "devDependencies": {
    "@google/clasp": "^2.4.2"
  }
}
